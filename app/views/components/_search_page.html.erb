<% if @employees %>
  <div class="search-results">
    <div class="row">
      <div class="col s12">
        <h4>Resultado da busca:</h4>
      </div>
    </div>

    <% if !@employees.empty? %>
      <%= render '/components/employee_list', employees: @employees %>
    <% else %>
      <div class="container">Não foi encontrado nenhum servidor</div>
    <% end %>
  </div>
<% end %>
